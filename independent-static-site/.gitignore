# public_html_ready.zip is ~209 MB, which exceeds GitHub's 100 MB
# per-file limit and cannot be pushed without Git LFS. The uncompressed
# public_html_ready/ folder is committed directly instead (no individual
# file inside it exceeds 100 MB) — see FINAL_QA_REPORT.md §10.
public_html_ready.zip
